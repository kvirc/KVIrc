# CMakeLists for ./caps

SET(PICSFILE kvi_mircimport.png)

IF(UNIX)
	IF(APPLE)
		INSTALL(FILES ${PICSFILE} DESTINATION ${INSTALL_PREFIX}/Contents/Resources/pics/)
	ELSE()
		# Assume linux
		INSTALL(FILES ${PICSFILE} DESTINATION ${INSTALL_PREFIX}/share/kvirc/${VERSION_BRANCH}/pics/)
	ENDIF()
ELSEIF(WIN32)
	#INSTALL(FILES ${CAPSFILE} DESTINATION ${INSTALL_PREFIX}/ ??? /${VERSION_BRANCH}/pics/)
ENDIF()
