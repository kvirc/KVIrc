language: cpp
os:
    - linux
compiler:
    - gcc
install:
    - cat /proc/cpuinfo /proc/meminfo
    - lsb_release -a
    - sudo add-apt-repository -y ppa:smspillaz/cmake-2.8.12
    - sudo apt-get update
    - sudo apt-get install libperl-dev cmake
script:
    - mkdir build
    - cd build
    - cmake ..
    - make -j3
    - sudo make install
